<?xml version="1.0" encoding="utf-8"?>
<!-- I've changed this to a ConstraintLayout to make it easier to place the bottom bar and button. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#053B80"
    tools:context=".ReportItemActivity">

    <!-- This LinearLayout holds the original content and keeps it centered. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp"
        app:layout_constraintBottom_toTopOf="@+id/bottom_nav_bar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- I'm adding a title here to guide the user. -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            android:text="What would you like to report?"
            android:textAlignment="center"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- This is the button to report a lost item. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_report_lost"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:backgroundTint="@android:color/white"
            android:padding="16dp"
            android:text="Report a LOST Item"
            android:textColor="#053B80"
            android:textSize="16sp" />

        <!-- And this is the button to report a found item. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_report_found"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="Report a FOUND Item"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            app:strokeColor="@android:color/white" />

    </LinearLayout>

    <!-- This is the white bar at the bottom of the screen. -->
    <View
        android:id="@+id/bottom_nav_bar"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- This is the new Dashboard button, centered inside the white bar. -->
    <ImageButton
        android:id="@+id/btn_dashboard"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Dashboard"
        android:padding="4dp"
        android:scaleType="fitCenter"
        android:src="@drawable/homebuttom"
        app:layout_constraintBottom_toBottomOf="@id/bottom_nav_bar"
        app:layout_constraintEnd_toEndOf="@id/bottom_nav_bar"
        app:layout_constraintStart_toStartOf="@id/bottom_nav_bar"
        app:layout_constraintTop_toTopOf="@id/bottom_nav_bar" />

</androidx.constraintlayout.widget.ConstraintLayout>
