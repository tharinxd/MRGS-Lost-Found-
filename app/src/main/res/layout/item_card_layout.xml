<?xml version="1.0" encoding="utf-8"?>
<!-- This is the layout for one single card in my list of lost items. -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:contentPadding="16dp">

    <!-- I've changed this to a horizontal LinearLayout to hold the text and the new icon. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- This LinearLayout holds the text and takes up most of the space. -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- This will display the item's title, like "adidas 720". -->
            <TextView
                android:id="@+id/item_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/black"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="adidas 720" />

            <!-- This will display the date the item was lost. -->
            <TextView
                android:id="@+id/item_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@android:color/black"
                android:textSize="12sp"
                tools:text="01/04/2025" />

        </LinearLayout>

        <!-- This is the new info icon button. -->
        <ImageButton
            android:id="@+id/info_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="View item details"
            android:padding="8dp"
            android:src="@drawable/info" />
        <!-- TODO: You'll need to add an info icon named 'ic_info' to your drawable folder. -->

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
